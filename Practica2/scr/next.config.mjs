/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ["das-p2-backend.onrender.com"], // Permite imágenes externas si es necesario
    },
  };
  
  export default nextConfig;
  